const clientUrl = {
    auth: {
        login: `auth/login`,
        register: `auth/register`,
        forget: `auth/forget`,
    }
};

const adminUrl = {};

const commonUrl = {
    auth: {
        logout: `auth/logout`,
    }
};

export { clientUrl, adminUrl, commonUrl };