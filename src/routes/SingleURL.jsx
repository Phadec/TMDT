import {PUBLIC_URL} from '../path';

const singleURl = {
    general: {
        register: PUBLIC_URL.REGISTER,
        login: PUBLIC_URL.LOGIN,
        forget: PUBLIC_URL.FORGET,
    }
}

export default singleURl;